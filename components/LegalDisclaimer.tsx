export default function LegalDisclaimer() {
  return (
    <div className="bg-danger/10 border border-danger/30 rounded-xl px-4 py-3 mb-8 text-sm">
      <p className="font-semibold text-danger mb-1">This is placeholder text, not a real legal document.</p>
      <p className="text-paper/80">
        It was drafted to show the structure a Terms of Service / Privacy Policy needs, not to
        provide actual legal coverage. Have an actual lawyer review and replace this before
        launching to real users — this text creates no legal protection as written.
      </p>
    </div>
  );
}
